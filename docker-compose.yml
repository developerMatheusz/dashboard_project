version: "3.9"

services:
  web:
    build: .
    container_name: django_app
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
